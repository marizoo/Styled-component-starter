import styled from "styled-components";
import { GloCenter, GloPadding } from "../../styles/GlobalStyles";

export const ScFooter = styled.div`
    ${GloPadding};
    ${GloCenter};
    height: 200px;
    width: 100vw;
    background: var(--primary-bg);
    color: var(--primary-color);
`;
